#include <sys/stat.h>
#include <sys/types.h>
#include <stdio.h>

int main(){
    
    // Return value 0:successful, -1:fail

    // int mkdir(const char *pathname, mode_t mode); 

    // int rename(cosnt char *oldpath, const char *newpath)

    // int chdir(const char *path); change current working directory
}